import styled from 'styled-components';
import MuiCollapse from '@mui/material/Collapse';

export const StyleMuiCollapse = styled(MuiCollapse)`
    position: sticky;
    top: 70px;

    .MuiCollapse-wrapperInner {
        padding-left: 0.1rem;
    }
`;
